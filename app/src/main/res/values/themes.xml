<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Base application theme.
    This theme is what the app uses for the native splash screen.
    -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background, animated icon, and animation duration. -->
        <item name="windowSplashScreenBackground">@color/seed</item>

        <!-- Use windowSplashScreenAnimatedIcon to add either a drawable or an
             animated drawable. One of these is required. -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>

        <!-- Set the theme of the Activity that follows your splash screen. -->
        <item name="postSplashScreenTheme">@style/Theme.Asser</item>
    </style>

    <style name="Theme.Asser" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
